# 贡献指南

欢迎通过 Issue 提供可复现的问题，通过 Pull Request 提交改进。提交前说明问题、改变后的行为及验证方式。

1. 创建独立分支，安装 requirements.txt。
2. 修改模型时同时说明来源、参数、最早可知时点、确认/失效规则和近似边界。
3. 保持 BBI 后置隔离；不通过 BBI 改写冻结方向。不用模型票数代替综合判断。
4. 为有实质行为变化的计算提供合成测试，执行 `python -m pytest scripts/tests -q` 与 `python scripts/audit_sources.py`。
5. 不提交密钥、账户、真实持仓、私有讲义、商业字体或未经许可的行情快照。

贡献应是你有权提供的内容，并按本项目 MIT 许可分发。测试通过不证明策略盈利；声称性能改进需提供无前视偏差的独立评估。
